name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Pin ruff version to match .pre-commit-config.yaml (v0.15.0)
      - name: Install linters
        run: pip install "ruff==0.15.0"

      - name: Ruff check
        run: ruff check skills/ scripts/

      - name: Ruff format check
        run: ruff format --check skills/ scripts/

  test-skills:
    name: Test Skills
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        skill:
          - codebase-onboarding-generator
          - contract-reviewer
          - dual-axis-skill-reviewer
          - esg-reporter
          - financial-analyst
          - hearing-to-requirements-mapper
          - internal-audit-assistant
          - iso-implementation-guide
          - it-system-roi-analyzer
          - kpi-designer
          - meeting-asset-preparer
          - multi-file-log-correlator
          - production-schedule-optimizer
          - project-artifact-linker
          - project-completeness-scorer
          - qr-code-generator
          - shift-planner
          - skill-designer
          - skill-idea-miner
          - vendor-support-ticket-tracker
          - wbs-review-assistant
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"

      - name: Install dependencies
        run: pip install -e ".[dev]" pytest-cov

      - name: Test ${{ matrix.skill }}
        run: >
          python -m pytest skills/${{ matrix.skill }}/scripts/tests/ -v
          --cov=skills/${{ matrix.skill }}/scripts
          --cov-report=term-missing

  test-repo-scripts:
    name: Test Repo Scripts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"

      - name: Install dependencies
        run: pip install -e ".[dev]" pytest-cov

      - name: Test repo-level scripts
        run: >
          python -m pytest scripts/tests/ -v
          --cov=scripts
          --cov-report=term-missing

  security:
    name: Security
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install security tools
        run: pip install "bandit[toml]>=1.9.0"

      - name: Bandit SAST scan
        run: bandit -r skills/ scripts/ --exclude "*/tests/*" -ll --skip B104,B310,B314,B608

  docs-build:
    name: Docs Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # docs/Gemfile.lock pins jekyll 3.10.0 / liquid 4.0.4, so Ruby 3.3 is fine
      # here. Regenerating that lock requires Bundler 2.3.x — modern Bundler
      # resolves github-pages down to 222 / jekyll 3.9.0, whose liquid calls
      # String#tainted? and fails on Ruby >= 3.2. See docs/README.md.
      # `bundler: default` uses the Ruby 3.3 built-in rather than installing the
      # EOL 2.3.27 named in the lock's BUNDLED WITH; the frozen install is
      # unaffected because it resolves nothing.
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler: default
          bundler-cache: true
          working-directory: docs

      - name: Build Jekyll site
        working-directory: docs
        run: bundle exec jekyll build
        env:
          JEKYLL_ENV: production
